# 验证记录

更新：2026-09-15。下面区分可重复的代码检查、本机实际执行与仍需用户参与的验收。

当前源码：47 项 MoonBit 测试通过，8 个 Windows 部署端到端检查通过；启动器模拟检查通过。

## 已实际执行

- Windows 原生程序：Python 虚拟环境创建与程序正常退出；Node 依赖安装、启动和 HTTP 健康检查。
- 中文、空格路径中的部署；保存计划后续跑；拒绝越界候选编号。
- HTTP 验证结束后没有残留测试监听端口；超时返回非成功状态。
- Dockerfile 构建并运行 CLI；Compose 解析配置、运行 CLI、回收任务容器和网络。
- 公开 GitHub 搜索与真实 Git 克隆；记录官方来源和提交，工作树保持干净。
- Windows DPAPI 加密/解密使用合成测试值，未读取真实用户密钥。
- 中文启动器的模拟构建/调用、参数转发和进程级环境修改。
- 应用本地 Yarn 1.22.22 自动安装、版本登记、实际执行和登记卸载；既有 Node/Python/Git 保留。下载使用官方 npm registry，明确禁止全局安装。
- Git 两次失败后的官方 SHA 固定 ZIP 获取；安全归档可解压，路径穿越归档被拒绝。更新无变化时复用，版本变化时新建目录并保留原有修改。
- 中文报告诊断 BAT 实际调用成功；无入口项目输出 LEARN，不执行猜测命令。
- Compose 故意退出 7 被判为失败，缺少 Yarn 时执行记录为空，自动发现 Node 本地 URL 后验证成功。

自动化入口：`moon test --deny-warn`、`scripts/test-bootstrap.ps1`、`scripts/test-deployment.ps1`。后者创建独立临时目录并保留证据，不清理用户目录。

## 未据此证明

- 新机首次系统安装、UAC、软件许可、WSL 更新/重启和系统级卸载。这些流程需要对应环境及用户参与；本机复用已安装工具与本地 Yarn 卸载不等于系统安装/卸载通过。
- 实际付费模型请求和真实密钥配置窗口。单元测试验证数据边界，不代表服务商账户可用。
- 任意仓库、全部历史兼容策略或完整系统隔离。当前可控示例通过不能替代逐仓库验证。

默认不发布原始运行日志；报告可能含本机路径与目标项目输出，共享前需要检查。
